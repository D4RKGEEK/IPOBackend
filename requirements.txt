# IPO Aggregation Platform — production dependencies
# Generated from a clean .venv on 2026-05-30. Pin versions to keep installs reproducible.
# To upgrade: `.venv/bin/python -m pip install -U <pkg>` then refresh via `pip freeze`.

# ─── Web framework ───────────────────────────────────────────────
fastapi==0.136.3
starlette==1.2.0
uvicorn==0.48.0
python-multipart==0.0.29

# ─── Templating (dashboard) ──────────────────────────────────────
Jinja2==3.1.6
MarkupSafe==3.0.3

# ─── Configuration ───────────────────────────────────────────────
pydantic==2.13.4
pydantic_core==2.46.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ─── Database (SQLAlchemy — SQLite locally, Postgres in production) ────
SQLAlchemy==2.0.50
alembic>=1.13
psycopg[binary]>=3.2

# ─── HTTP client (scraping + LLM calls) ─────────────────────────
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
certifi==2026.5.20

# ─── HTML / XML parsing (SEBI, BSE-SME) ─────────────────────────
beautifulsoup4==4.14.3
soupsieve==2.8.4
lxml==6.1.1

# ─── PDF extraction (section_resolver) ──────────────────────────
PyMuPDF==1.27.2.3
pdfplumber==0.11.9
pdfminer.six==20251230
pypdfium2==5.8.0
pillow==12.2.0

# ─── Cloudflare R2 storage (boto3 — S3 compat) ──────────────────
boto3==1.43.18
botocore==1.43.18
s3transfer==0.18.0
jmespath==1.1.0

# ─── Misc deps pulled by the above (pinned so installs are deterministic) ──
annotated-types==0.7.0
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
cryptography==48.0.0
idna==3.17
pycparser==3.0
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.7.0
